spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/voidcube_dev
    username: postgres
    password: password
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    show-sql: true