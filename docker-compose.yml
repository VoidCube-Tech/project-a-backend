services:
  postgres:
    image: postgres:16-alpine
    container_name: voidcube_pg

    ports:
      - "5432:5432"
    
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password 
      POSTGRES_DB: voidcube_dev
